# Text Editor — расширяемый редактор ✍️

Визуальный редактор, который поддерживает форматирование текста, картинки, видео, формулы.

## ✨ Ключевая функциональность

- Полноценный тулбар с пресетами `simple` с базовой функциональностью и `full` с полной.
- Встроенные стили шрифтов: гарнитура, размер, начертания, цвета текста и фона.
- Поддержка списков, цитат, блоков кода, выравнивания и управления отступами.
- Вставка ссылок, картинок, видео, математических формул (LaTeX).
- Возможность получить контент в HTML/JSON/Text формате при обновлении.

## 🛠️ Технологический стек

- **Интерфейс:** React, TypeScript, CSS, TipTap Editor
- **Инструменты разработки:** Node.js, NPM Scripts, Storybook, Vite.
- **Качество и стиль кода:** ESLint, Stylelint, Prettier, строгие правила TypeScript.

## 🚀 Установка и запуск

1. Установите Node.js версии 22 или новее.
2. Склонируйте репозиторий и перейдите в директорию проекта:

   ```bash
   git clone https://github.com/IrinaRyazanskaya/text-editor.git
   cd text-editor
   ```

3. Установите зависимости:

   ```bash
   npm install
   ```

4. Запустите песочницу для разработки:

   ```bash
   npm run storybook:start
   ```

   Storybook будет доступен по адресу `http://localhost:6006`.

5. Скрипты поддержки качества:

   ```bash
   npm run build-check           # Проверка корректности типов
   npm run format                # Автоформатирование с помощью Prettier
   npm run format-check          # Проверка правильности форматирования с помощью Prettier
   npm run lint                  # Комплексная проверка линтерами
   npm run lint:ts               # Проверка кода линтером ESLint
   npm run lint:styles           # Проверка стилей линтером Stylelint
   npm run lint-fix              # Автоматическое исправление ошибок линтинга
   npm run lint-fix:ts           # Автоисправление замечаний ESLint
   npm run lint-fix:styles       # Автоисправление замечаний Stylelint
   ```

## 📁 Структура проекта

```text
text-editor/
├── src/
│   ├── main.tsx                  # Точка входа React + Editor
│   ├── index.css                 # Глобальные стили и типографика
│   └── components/
│       └── editor/
│           ├── editor.tsx        # Провайдер EditorProvider, переключение variant'ов
│           ├── menu.tsx          # UI-тулбар, синхронизированный с состоянием редактора
│           ├── buttons.tsx       # Набор action-кнопок (bold, lists, media и т.д.)
│           ├── selects.tsx       # Выбор гарнитуры, размера и стиля заголовка
│           ├── extensions/       # FontSize, Video, MathInline, Indent плагины для TipTap
│           └── hooks/            # useMenuSync, useHeadingReset и reactive-хук
├── public/                       # Иконки и статические ресурсы
├── package.json                  # Скрипты, зависимости и метаданные проекта
├── vite.config.ts                # Настройки сборщика Vite
├── tsconfig.json                 # Настройки TypeScript
├── tsconfig.app.json             # Настройки TypeScript для фронтенда
├── tsconfig.node.json            # Настройки TypeScript для конфигов
├── eslint.config.js              # Правила линтинга JS/TS
├── .editorconfig                 # Единые правила оформления кода
├── .gitignore                    # Игнорируемые Git-ом файлы
├── .prettierignore               # Исключения для Prettier
├── .prettierrc.json              # Конфигурация Prettier
├── .stylelintrc.json             # Правила линтинга стилей
└── .prettierrc.json              # Настройки автоформатирования
```

## 💡 Что реализовано / изучено

- Собран компонент с текстовым редактором, готовый для использования в CMS и других прилоложениях.
- Настроена песочница Storybook для разработки и тестирования отдельных состояний редактора.
- Сделаны кастомные TipTap-расширения (Video, Math, FontSize, Indent).

## 📚 Решённые задачи и технические челленджи

- **Видео:** расширение `Video` поддерживает вставку видео по ссылке.
- **Формулы :** Поддержана работа с математическими формулами на основе KaTeX.
- **Сброс форматирования:** кнопка очистки форматирования возвращают редактор в дефолтное состояние.
- **Синхронизация тулбара и выделения:** кастомные хуки `useMenuSync` и `useHeadingReset` избавляют
  от визуальных расхождений между выбранным текстом и активными контролами, что облегчает UX.
